{
  "name": "dnd-pack-attack",
  "version": "0.0.14",
  "description": "Quick D&D creature pack attack dice rolling utility.",
  "type": "module",
  "homepage": ".",
  "keywords": [
    "svelte",
    "vite",
    "dnd",
    "dice"
  ],
  "dependencies": {
    "assert": "^2.0.0",
    "express": "^4.18.2",
    "express-favicon": "^2.0.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "svelte": "^5.0.0",
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.1.2"
  },
  "build": {
    "licence": "gpl-3.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --input-type=module -e \"import('./src/pack-attack.js').then(() => console.log('Tests passed.')).catch(e => { console.error(e); process.exit(1); })\""
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
